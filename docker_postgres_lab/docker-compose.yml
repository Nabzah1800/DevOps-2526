version: '3.8'

services:
  postgres:
    build: .
    container_name: postgres-db
    environment:
      POSTGRES_PASSWORD: rootpass123
      POSTGRES_DB: testdb
      POSTGRES_USER: testuser
    ports:
      - "5432:5432"

volumes:
  postgres_data:
